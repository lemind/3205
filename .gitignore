Фулстек 3205 ТЗ.md

# Local Claude Code config (machine-specific tool permissions, not shared project config)
.claude/

# Test coverage reports (backend's Jest config writes here via coverageDirectory: "../coverage")
/coverage
